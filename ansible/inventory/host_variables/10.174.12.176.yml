---
# Host variables for k8s12-001 (Kubernetes Worker Node)
# IP: 10.174.12.176

# k3s version configuration
k3s_channel: stable

# Control plane server URL
k3s_server_url: "https://k8s.lemarchant.jacobhouse.ca:6443"

# Cluster token from vault
k3s_token: "{{ vault_k3s_token }}"

# Node labels
k3s_node_labels:
  - "topology.kubernetes.io/zone=lemarchant"
  - "workload=general"

# Install kubectl
k3s_install_kubectl: true

# Additional agent arguments (optional)
# k3s_agent_args:
#   - "--kubelet-arg=max-pods=110"
