---
# Choose which web server to use: 'nginx' or 'apache'
web_server: 'nginx'

# Sites configuration
sites: []

# Apache-specific configuration
apache_index_options:
  - FancyIndexing
  - NameWidth=*
  - HTMLTable
  - SuppressDescription

# Variables for backward compatibility
nginx_sites: "{{ sites }}"
